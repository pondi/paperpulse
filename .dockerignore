# VCS and editor files
.git
.gitignore
.gitattributes
.github
.idea
.vscode
*.swp
*.swo
*.DS_Store

# Hidden directories and files (catch-all for .gemini/, .junie/, etc.)
.*
!.dockerignore

# Environment / secrets
.env
.env.*
.envrc
*.pem
*.key
*.crt
*.pfx

# Node & frontend build artifacts
node_modules
npm-debug.log*
yarn-error.log*
pnpm-lock.yaml
public/hot
public/build

# Composer vendor from host (use container-built vendor instead)
vendor

# Laravel runtime data (recreated in container)
storage
storage/**
bootstrap/cache/*.php

# Tests and local tooling
tests
phpunit.xml
phpstan.neon

# Documentation and examples (not needed in production images)
docs
mkdocs.yml
example
*.md
*.sh

# Deployment configurations (k8s manifests contain secrets; mkdocs not needed in images)
deploy/k8s
deploy/k8s/**
deploy/mkdocs
deploy/mkdocs/**

# Local build outputs or caches
site
coverage
*.log
